# PROJECT_TRUTH.md — وثيقة الحقيقة الوحيدة

آخر تحديث: 2026-08-14. عند تعارض أي ملف `.md` آخر مع الكود أو Git: **الكود يفوز**.
هذا الملف لا يُقرأ بدلاً من فحص الكود عند الشك — فقط لتوفير نقطة انطلاق.

## 1. ما هو المشروع
عجلة جان (Gann Wheel) تحليلية للأسواق المالية، تُعرض كصفحة HTML تفاعلية،
مع جسر محلي يربطها بحساب تداول MT5 تجريبي لحقن السعر الحي وتنفيذ الأوامر.

## 2. المعمارية الفعلية

- **المنتج الفعلي:** [public/kawkabat-al-arqam-al-sihria-v489-amibroker-live-final-clean.html](public/kawkabat-al-arqam-al-sihria-v489-amibroker-live-final-clean.html).
  **مُثبَت.**
- **يُشغَّل بـ:** `python -m http.server 8080 --directory public`. **مُثبَت** —
  فحصت العملية الفعلية المستمعة على المنفذ 8080 وقت كتابة هذا الملف
  (`Get-CimInstance Win32_Process`)، السطر الحرفي كان
  `python.exe -m http.server 8080 --directory public`. لا Vite، لا npm، رغم
  وجود `vite.config.js` و`package.json` في الجذر (غير مُستخدَمين للتشغيل الفعلي).
- **سلسلة 44 إطاراً متداخلاً:** v489 → v451 → v450 → ~40 ملف `layer` مرقّم →
  v209 → v282 → wheel-v9. **مُثبَت** بتتبّع حي عبر console المتصفح (عمق 44،
  لا حظر في أي مستوى). حقل `input[aria-label="Value"]` الذي يقرأه `applyPrice()`
  موجود عند العمق 42 داخل
  [public/kawkabat-al-arqam-al-sihria-v282-bidirectional-unlimited-wheel-engine-base.html:27](public/kawkabat-al-arqam-al-sihria-v282-bidirectional-unlimited-wheel-engine-base.html).
  `applyPrice()` نفسها في
  [public/…v489…html:519](public/kawkabat-al-arqam-al-sihria-v489-amibroker-live-final-clean.html)،
  تبحث عبر السلسلة كاملة recursively (لا عمق ثابت مكتوب بالكود، البحث ديناميكي).
- **`src/` (React) و`server/` و`dist/`: راكدة.** **مُثبَت جزئياً** — `git log`:
  آخر لمسة لـ`server/` و`dist/` بتاريخ 2025-08-04 (أكثر من سنة). لكن `src/`
  آخر لمسة 2026-08-05 (قبل 9 أيام فقط من اليوم) — ليست ركوداً كاملاً بقدر ما
  هي **غير مُشغَّلة حالياً** (لا عملية `vite`/`node` نشطة تخدم شيئاً). `dist/`
  مُتتبَّع في Git رغم كونه ناتج بناء (`dist/index.html` وغيره ضمن الكوميتات).
- **جسر MT5:** [bridge/mt5_bridge.py](bridge/mt5_bridge.py) على `127.0.0.1:8771`.
  **مُثبَت ومُختبَر حياً** (انظر §4). قراءة + تنفيذ (`/order`, `/close`, `/arm`,
  `/panic`) خلف حارس حساب حقيقي (`trade_mode==0`) يُفحص عند كل طلب، ومفتاح
  تسليح لا يُستعاد بعد إعادة التشغيل.
- **جسر AmiBroker:** `127.0.0.1:8770`. **مُثبَت غير متصل** — فحصت المنفذ
  (`Get-NetTCPConnection -LocalPort 8770`) فلم يُرجِع شيئاً، أي لا عملية
  تستمع عليه الآن. الإشارات تُكتب يدوياً إلى `bridge/signal.json` (عبر
  `bridge/make_signal.py`) بديلاً مؤقتاً.

## 3. حقائق تصحح توثيقاً خاطئاً

- **IQFeed غير مستخدم إطلاقاً.** **مُثبَت.** وجدت 15 ملفاً بالجذر باسم يحوي
  "iqfeed" (`.md`, `.bat`, `.ps1`, `.js`) — كلها توثيق/سكربتات ميتة. مصدر
  السعر الحي المصمَّم فعلياً هو AmiBroker → `signal.json` → الجسر.
- **"V641" أسطورة توثيق، لا مرجع حقيقي.** **مُثبَت.** 11 ملف جذر باسم
  `STABLE_V641_*` (`.json/.txt/.md` وملفات بلا امتداد)، و15 كوميتاً في تاريخ
  Git يذكر "V641" في رسالته — لكن **لا يوجد** `public/v641.html` أو أي ملف
  بهذا الاسم فعلياً على القرص. أرشيف نصوص، لا نسخة تشغيلية.
- **`BUILD_V293` و`STABLE_V641` كرقمَي إصدار لا يعنيان شيئاً تشغيلياً.**
  **مُستنتَج** من غياب أي ربط بينهما وبين اسم ملف فعلي في `public/`. المرجع
  الوحيد للنسخ = **Git tags** (§4).
- **~80 ملف `.md` بالجذر.** **مُثبَت بالعدّ الفعلي: 76** ملف `.md` + 55 ملف
  `.html` تقارير/اختبار إضافية بالجذر (خارج `public/`) — لم أقرأ محتواها
  (ممنوع صراحة)، هي أثر تاريخي، لا تُستخدم كمرجع.

## 4. الحالة الحالية

- الفرع الوحيد للعمل: `main`. `origin` = المرجع. الشجرة نظيفة وقت كتابة هذا الملف.
- **الوسوم بترتيبها الزمني الفعلي** (`git log -1 --format=%ci <tag>`؛ ترتيب
  المستخدم الأصلي في الطلب كان غير دقيق زمنياً، هذا هو المُثبَت):
  1. `baseline-v489` (2026-08-13) — نقطة مرجعية قبل أي تعديل على السلسلة المتداخلة.
  2. `mt5-bridge-v1` (2026-08-14 21:27) — جسر MT5 قراءة فقط (status/positions/signal).
  3. `local-frames-fixed` (2026-08-14 22:17) — تحويل 302 إطار من htmlpreview لمسارات محلية.
  4. `mt5-bridge-v1.1` (2026-08-14 22:33) — تحصين حارس `executed.json` (fail-safe) + 14 اختباراً.
  5. `mt5-bridge-v2` (2026-08-14 23:34) — تنفيذ فعلي: `/order`/`/close`/`/arm`/`/panic`، 32 اختباراً.
- **الحساب:** `53004995 @ ICMarketsSC-Demo` | `trade_mode=0` (DEMO) | `margin_mode=2` (Hedge). **مُثبَت** (`account_info()` مباشرة، مُستقلاً عن الجسر).
- **مُثبَت بالتنفيذ الفعلي وإغلاقه:** تذاكر `1861802442` / `1861803131` /
  `1861803133`، XAUUSD 0.01 لوت مع SL/TP مرفقين، فُتحت وأُغلقت 2026-08-14.
  الأثر في سجل MT5 نفسه يطابق تقرير الجسر حرفياً (تحقّق المستخدم بصرياً).
- **3 PRs مفتوحة، كلها من `saloqbi`، غير مدموجة.** **مُثبَت** عبر GitHub API
  مباشرة (`repos/.../pulls?state=open`): `#35` (Apply comfortable ornate
  frame V704)، `#27` (Robot diagnostic: center clock geometry)، `#2` (Build
  223: clean duplicate UI layers).
- **455 فرعاً بعيداً غير مصنَّف.** **مُثبَت بالعدّ الفعلي** (`git branch -r | wc -l`
  = 455؛ تقدير "~300" في الطلب الأصلي كان أقل من الواقع). **لا تُحذف أي منها.**

## 5. سجل المخاطر

| المخاطرة | الدليل | الأثر |
|---|---|---|
| **عمق 44 في سلسلة الإطارات** | تتبّع حي، §2 | أكبر خطر بنيوي. كل تحديث سعر يمر عبر 44 مستند متداخل؛ مصدر الوميض المحتمل وبطء أي تحديث DOM. |
| **استطلاع كل 500ms** | `public/…v489…html:626` `setInterval(...,500)` | يفسّر تحديدياً حجم/تكرار الوميض المذكور — ثابت في الكود، لم يُغيَّر. |
| **حساب Hedge لا يمنع التكرار** | `account_info().margin_mode==2` مُثبَت مباشرة | MT5 نفسه يقبل صفقتين متطابقتين بلا اعتراض. الحارس الوحيد: `executed.json` — الكتابة **قبل** `order_send` عمداً في [bridge/mt5_bridge.py:520](bridge/mt5_bridge.py) (`append_executed_id` قبل استدعاء `order_send`). |
| **7.7 ثانية حجب عند انقطاع MT5** | موثَّق ومُختبَر سابقاً في `bridge/README.md` (قيود معروفة) | مقبول لنقاط القراءة. **خطر مباشر** لو بُنيت حلقة تنفيذ تلقائي (استطلاع `signal.json` → `order_send` تلقائياً) قبل معالجته — لم تُعالَج بعد، غير موجودة في هذا المستودع. |
| **`applyPrice` غير مُختبَر بحقن سعر حي فعلي** | السلسلة سليمة الآن (§2)، لكن لم يُشغَّل جسر AmiBroker مطلقاً (المنفذ 8770 فارغ، §2) | الدالة موجودة وصحيحة بنيوياً (تصل لحقل Value)، لكن **لم يُثبَت** أنها تستقبل وتطبّق سعراً حياً فعلياً من AmiBroker — لا مصدر بيانات متصل بعد. |
| **اعتماد MongoDB نصي في مستودع عام** | [server/server.js:18](server/server.js) — رابط Atlas يتضمن مستخدماً وكلمة مرور بصيغة نصية صريحة | تسريب اعتماد فعلي في تاريخ Git لمستودع عام. لم يُبدَّل حتى وقت كتابة هذا الملف (`server/` راكد منذ 2025-08-04 — الاعتماد نفسه لم يُدوَّر). **يحتاج إجراءً بشرياً (تدوير كلمة المرور) لا كودياً.** |
| **ملف حلقة ذاتية** | [public/kawkabat-al-arqam-al-sihria-approved-1-360-all-days-zodiac-balanced-v9.html](public/kawkabat-al-arqam-al-sihria-approved-1-360-all-days-zodiac-balanced-v9.html) — لا يزال يحتوي مرجعي htmlpreview (تحقّقت: لا يزال 2 كما تُرك عمداً) | `meta refresh` + `window.location.replace` يشيران لنفس اسم الملف. فتحه محلياً = حلقة تحميل ذاتية لا نهائية. لا تفتحه، لا تُصلحه بنفس نمط بقية الملفات (سيُنشئ الحلقة). |
| **455 فرعاً بعيداً غير مصنَّف** | `git branch -r` | لا معرفة بمحتواها أو حداثتها دون فحص فردي. **لا تُحذف** أي منها دون تفويض صريح. |
| **3 PRs مفتوحة غير مدموجة** | GitHub API، §4 | تغييرات محتملة معلَّقة خارج `main` — لم تُفحَص للتعارض مع الحالة الحالية. |

## 6. قواعد العمل

1. **افتح المتصفح قبل `git commit`، لا بعده.** القاعدة الأهم في المشروع.
2. **ممنوع إنشاء ملف `vNNN` جديد.** الإصدارات بـ `git tag` حصراً.
3. ملفات عربية UTF-8 على Windows: `[IO.File]::ReadAllText/WriteAllText` مع
   `[Text.UTF8Encoding]::new($false)`. ممنوع `Get-Content`/`Set-Content` أو
   الأنبوب على ملف عربي — يتلف النص نهائياً.
4. PowerShell 5.1 لا يدعم `&&`. أوامر منفصلة أو `if ($?) { }`.
5. `git push origin HEAD` (لا `main` حرفياً) — يمنع الدفع من فرع خاطئ بالخطأ.
6. لا تحذف فرعاً أو ملف `vNNN` مهما بدا قديماً، دون تفويض صريح.
7. لا `order_send` تلقائي بلا `ARM` صريح + حارس حساب حقيقي مفحوص عند كل أمر.

## 7. الخطوة التالية المباشرة

آخر ما أُنجز: تنفيذ فعلي على MT5 (`mt5-bridge-v2`) — صفقات حقيقية فُتحت
وأُغلقت وتحقّق المستخدم بصرياً من مطابقتها لسجل MT5 (§4).
البند التالي المتفق عليه: إضافة أيقونة حالة اتصال MT5 إلى شريط أدوات v489
(لم تبدأ بعد) — الآن ممكنة بما أن السلسلة مفتوحة ومسار الحقن مُثبَت العمل.
